apiVersion: v1
kind: Pod
metadata: 
  name: myapp-Pod
  labels: 
    app: myapp

spec:
  containers:
    - name: myapp
      image: stacksimplify/kubenginx:1.0.0
      ports
        - containerPort: 80 

    